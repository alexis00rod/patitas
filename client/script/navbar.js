// Navbar elementos
const navbarButton = document.getElementById("navbar-button");
const navbarContainer = document.getElementById("navbar-container");

// Funcion abrir navbar
const openNavbar = () => {
  navbarContainer.classList.toggle("open");
};